apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: edge-app

spec:
  ingressClassName: traefik

  rules:
  - host: edge-app.192.168.56.102.sslip.io 
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: edge-app
            port:
              number: 80

